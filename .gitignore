# Local configuration
.env
.env.local
!.env.example
.env*.local

# Frontend
node_modules/
dist/
FRONTEND/node_modules/
FRONTEND/dist/
FRONTEND/playwright-report/
FRONTEND/test-results/

# Exploración visual descartable del pipeline de frontendengineer (specs,
# HTML de variantes, capturas, boards de comparación) — vive fuera de
# src/, se borra al cerrar cada ciclo, nunca se comitea.
.design-scratch/

# Backend del equipo: repositorio Git independiente, clonado localmente
# junto al frontend para integración sin mezclar historiales.
BACKEND/

# Local analysis & IDE artifacts
graphify-out/
.agents/
.claude/
.run/

# OS and editors
.DS_Store
desktop.ini
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
.run
.tmp
.test-cache
# Exploracion de diseno desechable (fase 4 de frontendengineer)
.design-scratch/
